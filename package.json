{
  "name": "monolog",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "postinstall": "node -e \"const cp = require('child_process'); try { cp.execSync('node node_modules/electron/install.js', { stdio: 'ignore' }); } catch (e) {}\"",
    "dev": "npm run dev -w apps/desktop",
    "build": "npm run build -w apps/desktop",
    "typecheck": "npm run typecheck -w apps/desktop",
    "lint": "npm run lint -w apps/desktop",
    "format": "npm run format -w apps/desktop"
  }
}
